(section
  (title "SDL.Video.Annex")
  (contents)

  (para "The " (item ada_package "SDL.Video.Annex") " package
    provides various useful extensions to the standard "
    (item ada_package "SDL.Video") " package.")

  (include "a-sdl-video-annex-data_size.ud"))
