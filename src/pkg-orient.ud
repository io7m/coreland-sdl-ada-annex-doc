(section
  (title "Orientation")

  (subsection
    (title "Overview")

    (para "This package provides a set of utility packages for "
      (link-ext "http://coreland.ath.cx/code/sdl-ada" "sdl-ada") ".")

    (para "It is is implemented as a set of child packages built
      off of the main " (item ada_package "SDL") " packages."))

) ; end of section
